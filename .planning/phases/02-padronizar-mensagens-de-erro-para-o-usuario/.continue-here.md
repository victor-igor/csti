---
context: phase
phase: 02-padronizar-mensagens-de-erro-para-o-usuario
task: null
total_tasks: null
status: context_captured
last_updated: 2026-05-11T23:09:14.948Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_Nenhuma constraint bloqueante identificada nesta sessão._

<current_state>
Fase 2 com CONTEXT.md completo (discuss-phase concluído). Ainda sem planos — próximo passo é /gsd-plan-phase 2.

O contexto da sessão estava em ~80% quando pausamos, por isso o /clear antes de continuar.
</current_state>

<completed_work>

- Phase 2 adicionada ao roadmap
- discuss-phase 2 executado completamente — 4 áreas discutidas, 10 decisões capturadas em `02-CONTEXT.md`
- Bug fix concluído: solicitações do cliente não apareciam para prestador
  - Causa: DB default `status = 'aberta'`, RLS do prestador só libera `'aguardando_orcamento'`
  - Correção: `useCreateSolicitacao` agora insere `status: 'aguardando_orcamento'` explicitamente
  - DB default alterado para `'aguardando_orcamento'`
  - 2 registros existentes migrados no banco
</completed_work>

<remaining_work>

- Rodar `/gsd-plan-phase 2` — criar planos de execução
- Rodar `/gsd-execute-phase 2` — implementar as correções de erro
</remaining_work>

<decisions_made>

**D-01:** Mensagens Zod nos schemas com `invalid_type_error`, `required_error` — padrão de mercado
**D-02:** Campos obrigatórios com mensagem específica por campo ("Informe o título da solicitação")
**D-03:** `z.coerce.number()` para campos numéricos — resolve "Invalid input: expected number, received string"
**D-04:** `src/lib/errorUtils.ts` com `parseApiError(error)` — centraliza tradução de erros Supabase
**D-05:** Mapear apenas erros comuns: sem conexão, email duplicado (23505), sem permissão (42501)
**D-06:** Remover padrão `error.message || 'fallback'` de todos os hooks
**D-07:** Tom claro e direto — diz o que fazer, não o que errou
**D-08:** 100% português BR
**D-09:** `ErrorState` com `onRetry` + texto contextual para todos os estados `isError`
**D-10:** Texto de erro de carregamento menciona o recurso ("Não foi possível carregar as solicitações")
</decisions_made>

<blockers>
Nenhum bloqueante.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-padronizar-mensagens-de-erro-para-o-usuario/02-CONTEXT.md` — decisões D-01 a D-10 que guiam o planejamento
2. `src/lib/errorUtils.ts` — arquivo a ser criado (não existe ainda)
3. `src/features/orcamento/OrcamentoFormPage.tsx` — caso mais urgente (bug "Invalid input" reportado)

## Infrastructure State
- Supabase: DB default de `solicitacoes_orcamento.status` já alterado para `'aguardando_orcamento'`
- Sem servidor local rodando

<context>
Sessão focou em: (1) diagnosticar bug de solicitações não aparecendo para prestador — corrigido; (2) configurar Fase 2 completa com discuss-phase. O CONTEXT.md capturou as decisões arquiteturais corretas de mercado para padronização de erros.
</context>

<next_action>
Fazer `/clear` primeiro (contexto estava alto), depois rodar `/gsd-plan-phase 2`
</next_action>
